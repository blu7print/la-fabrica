# Basura del sistema
.DS_Store
Thumbs.db
desktop.ini

# Nunca subas accesos. En esta carpeta no van claves, pero si alguna vez
# cae un archivo asi, que no salga de tu computadora.
.env
.env.*
*.key
*.pem
credentials.json
service-account*.json
